package otoole.scott.androidtest

import retrofit2.HttpException

fun handleError(e: Throwable) {

    ////////////////////////////////////////////////////////////////////////////////////////
    //notify Crashlytics or other crash reporting of error here, try to recover if possible
    ////////////////////////////////////////////////////////////////////////////////////////
    println(e.localizedMessage)
}